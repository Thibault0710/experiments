# Experiments

A small repository gathering various experimental ideas.

## VAE
Contains comparisons between a classic VAE (with KL divergence loss) and a VAE trained with a SigReg loss.

## Graph-FM
Contains experiments on flow matching for graphs. The goal is to generate images on graphs with random mesh topology.
